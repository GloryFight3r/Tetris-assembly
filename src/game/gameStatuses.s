/* shows the leaderboard
*
*
*/
leaderBoard:
	pushq %rbp
	movq %rsp, %rbp

	movq %rbp, %rsp
	popq %rbp
	ret

/* saves the current score
*
*
*/
saveScore:
	pushq %rbp
	movq %rsp, %rbp

	movq %rbp, %rsp
	popq %rbp
	ret

/* shows main menu
*
*
*/
mainMenu:
	pushq %rbp
	movq %rsp, %rbp

	movq %rbp, %rsp
	popq %rbp
	ret
